<Window x:Class="DataMonitoringSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataMonitoringSystem"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>

        </Grid.RowDefinitions>
        <!-- 타이틀 Label (모서리 둥글게) -->
            <Border Grid.ColumnSpan="3"
            Background="#FF2D2D2D"
            CornerRadius="20"
            Padding="10"
            Grid.Row="0"
            Margin="0,0,0,20">
            <Label Name="Title" Content="주조 설비 가동 현황"
           Foreground="White"
           FontSize="24"
           FontWeight="Bold"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"/>
        </Border>
        <Frame Grid.Row="1" x:Name="MainView" Content="" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
